fn main() -> number:
    let mutable sum = 0
    for i 1 to 10:
        sum = sum + i
    
    let mutable count = 5
    while count > 0:
        count = count - 1
    
    println("Done!")
    return 0

